name: CI

on:
  # Run on every push and PR — including feature branches — so a
  # non-compiling or failing-test commit is caught before it can be built
  # or shipped, not only on main/develop.
  pull_request:
  push:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  analyze-and-test:
    name: Analyze & Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          # Pin the Flutter (and therefore Dart) version so the toolchain —
          # notably `dart format` — is deterministic. Leaving `channel: stable`
          # unpinned lets a new stable's formatter reformat the repo and break
          # `dart format --set-exit-if-changed`. Bump deliberately, not by drift.
          flutter-version: 3.44.7
          channel: stable
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Check formatting
        run: dart format --set-exit-if-changed .

      - name: Analyze
        run: flutter analyze --no-fatal-infos

      - name: Run tests
        run: flutter test --coverage

      - name: Upload coverage
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: coverage/lcov.info
